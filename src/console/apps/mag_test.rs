use super::Token;

use ak09940a::non_blocking::AK09940A;
use esp_hal::peripherals::Peripherals;

pub async fn magnetometer_test() -> Result<(), ()> {
    // let ak09940a = AK09940A::new();
    Ok(())
}
